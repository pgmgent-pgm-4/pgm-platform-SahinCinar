import { RequestResult } from './util/types';
export declare class GraphQLRequestError<TResponse extends RequestResult = Response> extends Error {
    query: string;
    variables?: Record<string, unknown>;
    response: TResponse;
    constructor({ query, variables, response, message, }: {
        query: string;
        variables?: Record<string, unknown>;
        response: TResponse;
        message: string;
    });
}
//# sourceMappingURL=GraphQLRequestError.d.ts.map